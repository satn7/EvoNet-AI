# Copie para .env e ajuste valores
NODE_ENV=development
PORT=3000
JWT_SECRET=evonetai
TOKEN_EXPIRY=6900h


# DB (SQLite local por padrão)
DB_CLIENT=sqlite3
SQLITE_FILENAME=../data/evonet.db


# Para Postgres (opcional). Se usar, mude DB_CLIENT=pg
PG_CONNECTION_STRING=postgres://user:pass@host:5432/evonet


# Email (nodemailer). Use um SMTP real em produção.
SMTP_HOST=evonetai
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=evonetai@gmail.com
SMTP_PASS=evonetai
MAIL_FROM="EvoNet AI <no-reply@evonetai.com.br>"
MAIL_TO=contato@evonetai.com.br